const Cart = (cart) => {
  console.log(cart);
  return (
    <>
      <div>Hello!</div>
    </>
  );
};

export default Cart;

/* {products.map((product) => {
            return (
              <ProductPanel product={product} cart={cart} setCart={setCart} />
            );
          })}*/
